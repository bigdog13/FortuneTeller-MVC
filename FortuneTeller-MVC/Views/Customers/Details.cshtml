@model FortuneTeller_MVC.Models.Customer

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Customer</h4>
    <hr />
   
</div>
@Model.FirstName @Model.LastName will retire in @ViewBag.NumberOfYears years with @ViewBag.AmountOfMoney in the bank,
 a vacation home in [Location] and a @ViewBag.ModeOfTransportation.




<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.ID }) |
    @Html.ActionLink("Back to List", "Index")
</p>
